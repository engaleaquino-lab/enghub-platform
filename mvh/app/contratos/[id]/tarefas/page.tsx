"use client";
import {FormEvent,useEffect,useState} from "react";import {useParams} from "next/navigation";import AppShell from "@/components/AppShell";import ContractTabs from "@/components/ContractTabs";import Modal from "@/components/Modal";import {deleteRow,insertRow,listRows,updateRow} from "@/lib/supabase-data";
export default function Tasks(){const {id}=useParams<{id:string}>();const [items,setItems]=useState<any[]>([]),[open,setOpen]=useState(false);const load=async()=>setItems(await listRows("tasks",{contract_id:id}));useEffect(()=>{load()},[id]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await insertRow("tasks",{contract_id:id,title:String(f.get("title")||""),due_date:String(f.get("due_date")||"")||null,priority:String(f.get("priority")||"Média"),status:String(f.get("status")||"Pendente"),responsible:String(f.get("responsible")||"")});setOpen(false);await load()}return <AppShell><div className="topbar"><div><h1 className="section-title">Tarefas</h1><div className="muted">Pendências e responsabilidades</div></div><button className="btn" onClick={()=>setOpen(true)}>Nova tarefa</button></div><ContractTabs id={id}/><section className="card"><table className="table"><thead><tr><th>Tarefa</th><th>Responsável</th><th>Prazo</th><th>Prioridade</th><th>Status</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.title}</td><td>{x.responsible||"—"}</td><td>{x.due_date||"—"}</td><td>{x.priority}</td><td>{x.status}</td><td><div className="actions">{x.status!=="Concluída"&&<button className="btn secondary" onClick={async()=>{await updateRow("tasks",x.id,{status:"Concluída"});await load()}}>Concluir</button>}<button className="btn danger" onClick={async()=>{await deleteRow("tasks",x.id);await load()}}>Excluir</button></div></td></tr>)}{!items.length&&<tr><td colSpan={6} className="empty">Nenhuma tarefa cadastrada.</td></tr>}</tbody></table></section><Modal open={open} title="Nova tarefa" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={submit}><input className="input full" name="title" placeholder="Tarefa" required/><input className="input" name="responsible" placeholder="Responsável"/><input className="input" type="date" name="due_date"/><select className="input" name="priority"><option>Alta</option><option>Média</option><option>Baixa</option></select><select className="input" name="status"><option>Pendente</option><option>Em andamento</option><option>Concluída</option><option>Bloqueada</option></select><button className="btn full">Salvar</button></form></Modal></AppShell>}
